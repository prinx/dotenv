language: php

before_script:
    - composer self-update
    - composer install --no-interaction

script: phpunit

matrix:
    allow_failures:
        - php: 7.3
        - php: hhvm

    fast_finish: true

    notifications:
        on_success: never
        on_failure: always
